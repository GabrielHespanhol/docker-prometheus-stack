name: Docker image

on:
  push:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Build prometheus image
      run: docker build ./Docker/prometheus --tag "teste-prometheus:$(date +%s)"